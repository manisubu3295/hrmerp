# The Prisma CLI (generate/db push/studio) and the seed/backfill scripts in
# this package run with packages/database as their cwd, so they read THIS
# .env file — not apps/api/.env, even though both need the same value.
DATABASE_URL="postgresql://aadhirai:aadhirai_pass@localhost:5432/aadhirai"
